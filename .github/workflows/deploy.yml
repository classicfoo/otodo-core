name: Deploy to InfinityFree (FTP)

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install lftp
        run: sudo apt-get update && sudo apt-get install -y lftp

      - name: Deploy via FTP mirror
        env:
          FTP_HOST: ${{ secrets.FTP_HOST }}
          FTP_USER: ${{ secrets.FTP_USER }}
          FTP_PASS: ${{ secrets.FTP_PASS }}
          FTP_DIR: ${{ secrets.FTP_DIR }}
        run: |
          set -euo pipefail
          test -n "$FTP_DIR"
          lftp -u "$FTP_USER","$FTP_PASS" ftp://$FTP_HOST <<EOF2
          set ftp:ssl-allow no
          set net:timeout 20
          set net:max-retries 2
          set net:reconnect-interval-base 5
            mirror -R --verbose --delete --no-perms \
            --exclude-glob node_modules/** \
            --exclude-glob .env \
            --exclude-glob *.sqlite \
            --exclude-glob *.sqlite3 \
            --exclude-glob storage/*.sqlite \
            --exclude-glob storage/*.sqlite3 \
            --exclude .git/ \
            --exclude .github/ \
            --exclude data/ \
            ./ "$FTP_DIR"
          cd "$FTP_DIR"
          cls -la
          quit
          EOF2
